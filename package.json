{
  "name": "todo",
  "repository": "https://github.com/caesar003/todo.git",
  "author": "",
  "version": "1.0.6",
  "description": "A simple command-line to-do list manager built with Node.js. It allows users to manage their tasks with ease directly from the terminal, supporting operations like adding, deleting, marking tasks as done, listing tasks, and viewing task details.",
  "main": "bin/index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postbuild": "rm -rf ./bin/* && cp dist/todo.js ./bin/todo && chmod +x ./bin/todo"
  },
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.9.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }

}
